repos:
  - repo: local
    hooks:
      - id: export-requirements
        name: Export requirements
        entry: python -c "import subprocess; subprocess.run(['pip','freeze'], stdout=open('requirements.txt','w'))"
        language: system
        stages: [pre-push]